import style from "./OutSessionHeader.module.scss";


export default () => {
    return (
        <header class={style.header}>
            <span class={style.title}>
                Action
            </span>
        </header>
    )
}